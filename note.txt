# curl command to retrieve token 
curl -X POST "https://accounts.spotify.com/api/token" \
     -H "Content-Type: application/x-www-form-urlencoded" \
     -d "grant_type=client_credentials&client_id=<inser-client-ID>&client_secret=<insert-client-secret>"
